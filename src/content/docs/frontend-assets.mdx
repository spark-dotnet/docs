---
title: "Frontend Assets"
description: "Spark Frontend Assets"
---

## Introduction
While Blazor makes a lot possible in terms of UI, you will still need CSS & possibly JavaScript in some scenarios.

All Spark projects come with a CSS framework already setup for you. 

It also comes with AlpineJS available to use. AlpineJS a light weight JavaScript framework that is great for little UI interactions.

## CSS
Spark projects come with Tailwind CSS by default.

When creating a new Spark project, you can use other CSS frameworks by passing the <br class="hidden xl:block"/>`--css` option when running the `spark new [projectName]` command.
```bash
spark new MyApp --css bootstrap
```

### CSS Framework Options

**Tailwind CSS**
```bash
spark new MyApp --css tailwind
```
Learn more about Tailwind [here](https://tailwindcss.com/).

**Bootstrap CSS**
```bash
spark new MyApp --css bootstrap
```
Learn more about Bootstrap [here](https://getbootstrap.com/).

**Pico CSS**
```bash
spark new MyApp --css pico
```
Learn more about Pico [here](https://picocss.com/).

## Javascript
Spark project come with AlpineJS by default. 

AlpineJS is a javascript library that helps you create simple page interactions like mobile menus and toggling elements.

You can learn more about Alpine [here](https://alpinejs.dev/).